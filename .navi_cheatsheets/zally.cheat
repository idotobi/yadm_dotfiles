% swagger, openapi, web, rest, documentation, render

# start swagger-editor
docker run -p 8080:8080 swaggerapi/swagger-editor


% zally, web, rest, api

# start zally
cd ~/Documents/tools/zally/server
./gradlew bootRun
